class LearnipediaLessonsController < ApplicationController
  def destroy
    @lesson = LearnipediaLesson.find(params[:id])
    @learnipedia = @lesson.learnipedia
    @lesson.destroy

    unless @learnipedia.id.nil?
      redirect_to learnipedia_path(@learnipedia)
    end
  end
end
